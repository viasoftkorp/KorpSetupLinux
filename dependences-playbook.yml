---
- hosts: 127.0.0.1
  connection: local
  become: true
  
  tasks:

    - name: instalação de collection community.general
      community.general.ansible_galaxy_install:
        type: collection
        name: "{{ item }}"
      loop:
        - community.docker
