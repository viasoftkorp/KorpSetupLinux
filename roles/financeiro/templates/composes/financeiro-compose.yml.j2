version: "3.8"

x-extra_hosts:
  &default-extra_hosts
  - "db_mssql:$DB_MSSQL"
  - "testing_db_mssql:$TESTING_DB_MSSQL" 
  - "app_server:$APP_SERVER"
  - "{{ authority.api }}:$API_GATEWAY"
  - "{{ authority.frontend }}:$PORTAL_GATEWAY"
  - "{{ authority.cdn }}:$CDN_GATEWAY"

networks:
  servicos:
    external:
      name: servicos

services:
  viasoft-financeiro-core:
    image: "{{ docker_account }}/viasoft.financeiro.core:1.0.x{{ docker_image_suffix }}"
    container_name: "Viasoft.Financeiro.Core"
    restart: on-failure:10
    extra_hosts: *default-extra_hosts
    environment:
      - ON_PREMISE_MODE=true
      - USE_SERVERGC={{ use_servergc }}
      - URL_CONSUL=http://consul-server:8500
      - CERT_FILE_PATH={{ containers_cert_path }}
    networks:
      - servicos
    volumes:
      - "{{ certs_directory }}/:{{ certs_directory }}/"

  viasoft-financialaccounting-installment:
    image: "{{ docker_account }}/viasoft.financialaccounting.installment:2.2.x{{ docker_image_suffix }}"
    container_name: "Viasoft.FinancialAccounting.Installment"
    restart: on-failure:10
    extra_hosts: *default-extra_hosts
    environment:
      - ON_PREMISE_MODE=true
      - USE_SERVERGC={{ use_servergc }}
      - URL_CONSUL=http://consul-server:8500
      - CERT_FILE_PATH={{ containers_cert_path }}
    networks:
      - servicos
    volumes:
      - "{{ certs_directory }}/:{{ certs_directory }}/"

  viasoft-financialaccounting-accountsreceivable:
    image: "{{ docker_account }}/viasoft.financialaccounting.accountsreceivable:2.1.x{{ docker_image_suffix }}"
    container_name: "Viasoft.FinancialAccounting.AccountsReceivable"
    restart: on-failure:10
    extra_hosts: *default-extra_hosts
    environment:
      - ON_PREMISE_MODE=true
      - USE_SERVERGC={{ use_servergc }}
      - URL_CONSUL=http://consul-server:8500
      - CERT_FILE_PATH={{ containers_cert_path }}
    networks:
      - servicos
    volumes:
      - "{{ certs_directory }}/:{{ certs_directory }}/"

  korp-legacy-financeiro:
    image: "{{ docker_account }}/korp.legacy.financeiro:1.0.x{{ docker_image_suffix }}"
    container_name: "Korp.Legacy.Financeiro"
    restart: on-failure:10
    extra_hosts: *default-extra_hosts
    environment:
      - ON_PREMISE_MODE=true
      - USE_SERVERGC={{ use_servergc }}
      - URL_CONSUL=http://consul-server:8500
      - CERT_FILE_PATH={{ containers_cert_path }}
    networks:
      - servicos
    volumes:
      - "{{ certs_directory }}/:{{ certs_directory }}/"

  viasoft-financialaccounting-accountsreceivable:
    image: "{{ docker_account }}/viasoft.financialaccounting.accountsreceivable:2.1.x{{ docker_image_suffix }}"
    container_name: "Viasoft.FinancialAccounting.AccountsReceivable"
    restart: on-failure:10
    extra_hosts: *default-extra_hosts
    environment:
      - ON_PREMISE_MODE=true
      - URL_CONSUL=http://consul-server:8500
      - CERT_FILE_PATH={{ containers_cert_path }}
    networks:
      - servicos
    volumes:
      - "{{ certs_directory }}/:{{ certs_directory }}/"
