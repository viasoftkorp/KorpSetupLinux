{
    "Authorization": {
        "Secret": "{{ secret }}"
    },
    "AutoUpdateScheduler": "23:45",
    "ClientId": "{{ services_secrets['Korp.Legacy.TenantManagement'].ClientId }}",
    "ClientSecret": "{{ services_secrets['Korp.Legacy.TenantManagement'].ClientSecret }}",
    "ConnectionStrings": {
        "DefaultConnection": "host={{ postgres_container_name}} user={{ postgres.korp_user }} password={{ postgres.korp_password }} dbname=%s port=5432"
    },
    "PostgresDatabaseName": "korp.legacy.tenantmanagement"
}
