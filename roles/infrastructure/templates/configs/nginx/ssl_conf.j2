listen  443 ssl;

ssl_certificate     /etc/nginx/certs/cert.crt;
ssl_certificate_key /etc/nginx/certs/cert.key;
{% if certs.custom.has_pass_file %}
    ssl_password_file   /etc/nginx/certs/cert.pass;
{% endif %}